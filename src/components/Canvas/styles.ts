import styled from 'styled-components';

export const CanvasWrapper = styled.div`
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    z-index: -1;
`;

export const CanvasStage = styled.canvas`
    width: 100%;
    height: 100%;
`;